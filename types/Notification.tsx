export type Notification = {
    author: string,
    authorId: string,
    comment: string,
    commentId: string,
    post: string,
    publishDate: string,
    type: string,
    watched: boolean, 
}