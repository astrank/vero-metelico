export type Notification = {
    id: string,
    author: string,
    authorId: string,
    comment: string,
    commentId: string,
    post: string,
    postTitle: string,
    publishDate: string,
    type: string,
    watched: boolean, 
}